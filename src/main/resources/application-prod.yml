spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        #        show_sql: true
#        format_sql: true
  config:
    import:
      - classpath:application-prod-secret.yml

#logging.level:
#  org.hibernate.SQL: debug
#  org.hibernate.type: trace
